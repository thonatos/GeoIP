machine:
  services:
    - docker

dependencies:
  override:
    - docker info
    - docker build --rm=false -t thonatos/GeoIP:$CIRCLE_SHA1 .

test:
  override:
    - docker run -d -p 3000:3000 Geo-IP:$CIRCLE_SHA1; sleep 10
    - curl --retry 10 --retry-delay 5 -v http://localhost:3000
